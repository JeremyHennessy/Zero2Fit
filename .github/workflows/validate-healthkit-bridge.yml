name: Validate HealthKit Bridge

on:
  pull_request:
    paths:
      - 'ios/Zero2FitHealthBridge/**'
      - '.github/workflows/validate-healthkit-bridge.yml'
  push:
    branches: [main]
    paths:
      - 'ios/Zero2FitHealthBridge/**'
      - '.github/workflows/validate-healthkit-bridge.yml'

permissions:
  contents: read

jobs:
  build-ios-simulator:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check native acceptance-console contract
        working-directory: ios/Zero2FitHealthBridge
        run: |
          test -s Sources/SourceAcceptanceSummary.swift
          test -s Sources/ActivationStatus.swift
          test -s Tools/TestSourceAcceptanceSummary.swift
          test -s Tools/TestActivationReadiness.swift
          grep -q 'Physical source acceptance console' Sources/ContentView.swift
          grep -q 'Activation readiness' Sources/ContentView.swift
          grep -q 'Refresh private activation status' Sources/ContentView.swift
          grep -q 'Capture + sync last 24 hours' Sources/ContentView.swift
          grep -q 'Last background delivery' Sources/ContentView.swift
          grep -q 'Copy bundle ID' Sources/ContentView.swift
          grep -q 'sourceSummaries' Sources/BridgeViewModel.swift
          grep -q 'activationReadiness' Sources/BridgeViewModel.swift
          grep -q 'refreshActivationStatus' Sources/BridgeViewModel.swift
          grep -q 'lastBackgroundSyncAt' Sources/BridgeViewModel.swift
          grep -q 'fetchActivationStatus' Sources/SupabaseBridgeClient.swift
          grep -q '/rest/v1/device_source_verifications?select=' Sources/SupabaseBridgeClient.swift
          grep -q 'method: "GET"' Sources/SupabaseBridgeClient.swift
          grep -q 'sourceBundleId' Sources/SourceAcceptanceSummary.swift
          grep -q 'latestValue' Sources/SourceAcceptanceSummary.swift
          grep -q 'observedBundleIds.contains' Sources/ActivationStatus.swift
          grep -q 'verification(for: "zepp")' Sources/ActivationStatus.swift
          grep -q 'verification(for: "renpho")' Sources/ActivationStatus.swift
          if grep -R -n 'verifySource\|source_verification_status' Sources/ContentView.swift Sources/BridgeViewModel.swift Sources/SourceAcceptanceSummary.swift Sources/ActivationStatus.swift; then
            echo 'Native activation UI must not create or simulate source verification.'
            exit 1
          fi
          if grep -q '/rest/v1/device_source_verifications?on_conflict' Sources/SupabaseBridgeClient.swift; then
            echo 'Native bridge must never POST/upsert source verification rows.'
            exit 1
          fi

      - name: Test native source acceptance summary model
        working-directory: ios/Zero2FitHealthBridge
        run: |
          xcrun swiftc \
            Sources/BridgeModels.swift \
            Sources/SourceAcceptanceSummary.swift \
            Tools/TestSourceAcceptanceSummary.swift \
            -o /tmp/zero2fit-source-acceptance-test
          /tmp/zero2fit-source-acceptance-test

      - name: Test native activation readiness model
        working-directory: ios/Zero2FitHealthBridge
        run: |
          xcrun swiftc \
            Sources/BridgeModels.swift \
            Sources/ActivationStatus.swift \
            Tools/TestActivationReadiness.swift \
            -o /tmp/zero2fit-activation-readiness-test
          /tmp/zero2fit-activation-readiness-test

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        working-directory: ios/Zero2FitHealthBridge
        run: xcodegen generate

      - name: Build iOS simulator target
        working-directory: ios/Zero2FitHealthBridge
        run: |
          xcodebuild \
            -project Zero2FitHealthBridge.xcodeproj \
            -scheme Zero2FitHealthBridge \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .derived-data \
            CODE_SIGNING_ALLOWED=NO \
            build
